name: Build Artifacts

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main, dev ]

jobs:
  build:
    uses: magemonkeystudio/.github/.github/workflows/reusable-maven.yml@main
    secrets: inherit

